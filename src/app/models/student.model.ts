export interface Student {
    id: number,
    createdAt: string,
    name: string,
    surname: string,
    email: string,
    documentNumber: number,
    isActive: boolean,
    phoneNumber: number
}